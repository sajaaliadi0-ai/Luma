/* =========================================
   HOME PAGE
========================================= */

.home-page {
  width: 100%;
  min-height: 100vh;

  display: flex;
  flex-direction: column;
  align-items: center;

  padding: 30px 20px 50px;

  background: var(--bg);
  color: var(--text);

  box-sizing: border-box;
}

.home-page *,
.home-page *::before,
.home-page *::after {
  box-sizing: border-box;
}

/* =========================================
   TOP CREDIT
========================================= */

.home-top-credit {
  margin-top: 10px;

  color: var(--text-secondary);

  font-size: 13px;

  text-align: center;
}

.home-top-credit a {
  margin-left: 5px;

  color: var(--primary);

  text-decoration: none;

  font-weight: 600;
}

/* =========================================
   TEAM AVATARS
========================================= */

.home-team-orbs {
  display: flex;

  align-items: center;

  justify-content: center;

  margin-top: 65px;

  min-height: 65px;
}

.home-avatar-wrapper {
  position: relative;

  margin-left: -8px;
}

.home-avatar-wrapper:first-child {
  margin-left: 0;
}

.home-team-avatar {
  display: block;

  width: 52px;

  height: 52px;

  border: 3px solid var(--surface);

  border-radius: 50%;

  background-position: center;

  background-size: cover;

  background-repeat: no-repeat;

  cursor: pointer;

  transition:
    transform 0.3s ease,
    box-shadow 0.3s ease;
}

.home-team-avatar:hover {
  transform: translateY(-7px) scale(1.1);

  box-shadow: 0 10px 25px rgba(79, 110, 247, 0.3);
}

/* =========================================
   TOOLTIP
========================================= */

.home-agent-tooltip {
  position: absolute;

  left: 50%;

  bottom: calc(100% + 12px);

  z-index: 50;

  transform: translateX(-50%);

  padding: 7px 12px;

  border-radius: 8px;

  background: var(--text);

  color: var(--surface);

  white-space: nowrap;

  font-size: 12px;

  pointer-events: none;
}

.home-agent-tooltip::after {
  content: "";

  position: absolute;

  left: 50%;

  top: 100%;

  transform: translateX(-50%);

  border: 6px solid transparent;

  border-top-color: var(--text);
}

/* =========================================
   TITLE
========================================= */

.home-title {
  margin: 35px 0 30px;

  color: var(--text);

  font-size: clamp(28px, 4vw, 42px);

  line-height: 1.2;

  font-weight: 700;

  text-align: center;
}
/* =========================================
   MESSAGES
========================================= */

.home-sent-messages {
  width: min(680px, 90%);

  display: flex;

  flex-direction: column;

  gap: 12px;

  margin: 0 auto 20px;
}

.home-message {
  display: flex;

  flex-direction: column;

  width: fit-content;

  max-width: 75%;

  padding: 12px 16px;

  border-radius: 18px;

  font-size: 14px;

  line-height: 1.5;

  animation: homeMessagePop 0.35s ease;
}

.home-message.user {
  align-self: flex-end;

  border-radius: 18px 18px 4px 18px;

  background: var(--primary);

  color: #ffffff;
}

.home-message.assistant {
  align-self: flex-start;

  border-radius: 18px 18px 18px 4px;

  background: var(--hover);

  color: var(--text);
}

.home-message b {
  margin-bottom: 4px;

  font-size: 12px;
}

@keyframes homeMessagePop {
  from {
    opacity: 0;

    transform: translateY(15px) scale(0.97);
  }

  to {
    opacity: 1;

    transform: translateY(0) scale(1);
  }
}

/* =========================================
   TYPING
========================================= */

.home-typing-dots {
  display: flex;

  gap: 4px;

  margin-top: 4px;
}

.home-typing-dots i {
  width: 5px;

  height: 5px;

  border-radius: 50%;

  background: var(--text-secondary);

  animation: homeTyping 1s infinite ease-in-out;
}

.home-typing-dots i:nth-child(2) {
  animation-delay: 0.15s;
}

.home-typing-dots i:nth-child(3) {
  animation-delay: 0.3s;
}

@keyframes homeTyping {
  0%,
  60%,
  100% {
    opacity: 0.3;

    transform: translateY(0);
  }

  30% {
    opacity: 1;

    transform: translateY(-3px);
  }
}

/* =========================================
   PROMPT
========================================= */

.home-prompt {
  position: relative;

  width: min(680px, 90%);

  overflow: visible;

  border: 1px solid var(--border);

  border-radius: 18px;

  background: var(--surface);

  box-shadow: var(--shadow);

  transition:
    border-color 0.25s ease,
    box-shadow 0.25s ease,
    transform 0.25s ease;
}

.home-prompt:focus-within {
  border-color: var(--primary);

  box-shadow: 0 10px 30px rgba(101, 117, 212, 0.12);
}

.home-prompt:hover {
  transform: translateY(-2px);

  box-shadow: var(--shadow);
}

/* =========================================
   INPUT
========================================= */

.home-prompt input {
  display: block;

  width: 100%;

  padding: 18px 18px 12px;

  border: 0;

  outline: none;

  background: transparent;

  color: var(--text);

  font-family: Inter, Arial, sans-serif;

  font-size: 14px;
}

.home-prompt input::placeholder {
  color: var(--text-secondary);
}

/* =========================================
   PROMPT FOOTER
========================================= */

.home-prompt footer {
  position: relative;

  display: flex;

  align-items: center;

  gap: 6px;

  padding: 10px 12px 12px;
}

.home-prompt footer button {
  display: flex;

  align-items: center;

  justify-content: center;

  min-width: 34px;

  height: 34px;

  padding: 0 9px;

  border: 0;

  border-radius: 9px;

  background: transparent;

  color: var(--text-secondary);

  cursor: pointer;

  font-size: 13px;

  transition:
    background 0.2s ease,
    transform 0.2s ease;
}

.home-prompt footer button:hover {
  background: var(--hover);

  transform: translateY(-1px);
}

.home-prompt-spacer {
  flex: 1;
}

/* =========================================
   PROMPT ACTION
========================================= */

.home-prompt-action {
  position: relative;

  display: flex;
}

.home-prompt-action > button {
  gap: 5px;
}
/* =========================================
   PLUS / THEME MENUS
========================================= */

.home-plus-menu,
.home-theme-menu {
  position: absolute;

  bottom: calc(100% + 12px);

  left: 0;

  z-index: 100;

  display: flex;

  flex-direction: column;

  gap: 4px;

  width: 160px;

  padding: 8px;

  border: 1px solid var(--border);

  border-radius: 12px;

  background: var(--surface);

  box-shadow: var(--shadow);
}

.home-theme-menu {
  width: 100px;
}

.home-plus-menu button,
.home-theme-menu button {
  width: 100%;

  justify-content: flex-start;

  padding: 9px 10px;

  border: 0;

  border-radius: 8px;

  background: transparent;

  color: var(--text);

  text-align: left;

  cursor: pointer;
}

.home-plus-menu button:hover,
.home-theme-menu button:hover {
  background: var(--hover);
}

/* =========================================
   BUILD BUTTON
========================================= */

.home-build-button {
  gap: 5px;
}

/* =========================================
   SEND BUTTON
========================================= */

.home-go-button {
  min-width: 38px !important;

  height: 38px !important;

  border-radius: 50% !important;

  background: var(--primary) !important;

  color: #ffffff !important;

  font-size: 18px !important;
}

.home-go-button:hover {
  background: var(--primary-hover) !important;
}

/* =========================================
   RECORDING
========================================= */

.home-recording {
  color: var(--primary) !important;

  background: var(--hover) !important;
}

/* =========================================
   TOOLS NOTICE
========================================= */

.home-tools-notice {
  display: flex;

  align-items: center;

  gap: 9px;

  padding: 10px 14px;

  border-top: 1px solid var(--border);

  color: var(--text-secondary);

  font-size: 12px;
}

.home-tools-notice > span {
  flex: 1;
}

.home-tool-dots {
  letter-spacing: 2px;

  color: var(--text-secondary);
}

.home-tools-notice > button {
  width: 25px !important;

  min-width: 25px !important;

  height: 25px !important;

  padding: 0 !important;
}
/* =========================================
   TABLET
========================================= */

@media (max-width: 768px) {
  .home-page {
    padding: 25px 15px 40px;
  }

  .home-team-orbs {
    margin-top: 45px;
  }

  .home-team-avatar {
    width: 45px;

    height: 45px;
  }

  .home-title {
    margin-top: 28px;
  }

  .home-prompt,
  .home-sent-messages {
    width: 95%;
  }
}

/* =========================================
   MOBILE
========================================= */

@media (max-width: 480px) {
  .home-page {
    min-height: 100vh;

    padding: 20px 10px 30px;
  }

  .home-top-credit {
    font-size: 12px;
  }

  .home-team-orbs {
    margin-top: 40px;
  }

  .home-team-avatar {
    width: 38px;

    height: 38px;

    border-width: 2px;
  }

  .home-avatar-wrapper {
    margin-left: -6px;
  }

  .home-title {
    width: 95%;

    margin: 25px 0;

    font-size: 25px;
  }

  .home-prompt {
    width: 100%;

    border-radius: 16px;
  }

  .home-prompt footer {
    flex-wrap: wrap;
  }

  .home-prompt-spacer {
    display: none;
  }

  .home-build-button {
    margin-left: auto;
  }

  .home-tools-notice {
    flex-wrap: wrap;
  }

  .home-tools-notice > span {
    flex: unset;
  }

  .home-tool-dots {
    display: none;
  }

  .home-sent-messages {
    width: 100%;
  }

  .home-message {
    max-width: 85%;
  }
}

/* =========================================
   VERY SMALL SCREENS
========================================= */

@media (max-width: 360px) {
  .home-team-avatar {
    width: 34px;

    height: 34px;
  }

  .home-title {
    font-size: 22px;
  }

  .home-prompt footer button {
    min-width: 30px;

    padding: 0 6px;
  }
}
